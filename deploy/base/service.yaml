apiVersion: v1
kind: Service
metadata:
  name: ai-ticket-intake
  labels:
    app: ai-ticket-intake
spec:
  selector:
    app: ai-ticket-intake
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
