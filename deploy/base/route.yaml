apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ai-ticket-intake
spec:
  to:
    kind: Service
    name: ai-ticket-intake
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
